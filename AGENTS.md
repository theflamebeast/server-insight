# AGENTS.md

This file only points. Read both of these before doing anything in this repo:

1. **`CLAUDE.md`**, next to this file — this project's facts: stack, layout,
   danger zones, the build/test commands, the done-checklist gates and what
   needs review.
2. **`C:\Users\anish\.claude\CLAUDE.md`, section "Every repo"** — the workflow
   rules shared by all of this owner's repos: ask first when anything is
   unclear, open the final message with the done-checklist, show evidence rather
   than assertions, comment only the non-obvious WHY, commit and push when done,
   and suggest follow-ups without starting them.

`CLAUDE.md` is written for Claude Code, so it names Claude tools
(`AskUserQuestion`, the `Skill` tool, `/code-review`). Read those as the intent
and use your own equivalent: ask the user, follow the named procedure, and hand
the review to the owner rather than claiming it passed.

Nothing else belongs in this file. A second copy of the facts drifts; change
`CLAUDE.md` instead.
